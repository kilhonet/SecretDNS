# SecretDNS

**DNS over HTTPS と SNI 断片化でインターネット検閲(DPI)を回避する、ワンクリックで有効にできる Windows 用フリーソフト。**

[English](README.md) · [한국어](README.ko.md) · [简体中文](README.zh-CN.md) · 日本語 · [Español](README.es.md) · [Português (Brasil)](README.pt-BR.md) · [Français](README.fr.md)

> この文書は翻訳版です。内容に相違がある場合は[韓国語版](README.ko.md)が正となります。

![Platform](https://img.shields.io/badge/platform-Windows%2010%20%2F%2011-0078D4)
![License](https://img.shields.io/badge/license-Freeware-brightgreen)
![Version](https://img.shields.io/badge/version-4.0.5-blue)
[![Download](https://img.shields.io/badge/download-kilho.net-orange)](https://down.kilho.net/secretdns?lang=ja)

![SecretDNS 画面](images/secretdns-en.webp)

## 概要

Web サイトを開くとき、コンピューターは 2 つの情報を平文で送ります。サイトの IP アドレスを問い合わせる **DNS クエリ**と、HTTPS 接続の最初のパケットに含まれる**サイト名(SNI)**です。通信事業者やネットワーク管理者の検閲装置(DPI)はこの 2 つを読み取ってどのサイトにアクセスしているかを把握し、必要に応じて接続を切断したり警告ページへ誘導したりします。

SecretDNS は **[実行]** のワンクリックでこの 2 つの穴をふさぎます。

- **DNS** — コンピューターの DNS クエリを Cloudflare などのサーバーで**暗号化(DoH)**して処理します。Windows のネットワーク設定には一切触れないため、プログラムを停止すればその場で元に戻り、実行中にコンピューターが突然落ちても、再起動すれば何の痕跡もなくいつも通りインターネットが使えます。
- **SNI** — HTTPS 接続時に**サイト名が検閲装置に見えないように**処理します。サイトへの接続自体は普段どおりで、名前の部分だけを処理するため速度低下はほとんどありません。

銀行や決済など断片化すると動作しなくなるサイトは内蔵の**例外リスト**でそのまま通し、**レポート**でどのサイトがどう処理されたかを確認できます。インターネットの自由が保障されていない国でよく見られる **451 エラー**(サイト側がその国からの接続自体を拒否するもの)のように DNS の回避では開けないサイトは、**混合プロキシ**でそのサイトだけ他国経由で接続できます。指定したドメイン以外は経由しないため、それ以外のインターネット速度はそのままです。

SecretDNS は VPN ではありません。IP アドレスを隠したり通信全体を暗号化したりはせず、検閲に使われる DNS と SNI の 2 か所だけを処理します。

## 主な機能

- **ワンクリック** — ホーム画面の **[実行]** を押すだけ。トレイアイコンのクリックでもオン/オフできます。
- **DNS over HTTPS** — 既定は Cloudflare。サーバーを自分で追加し複数にチェックを入れておくと自動で切り替えて使います。暗号化なしで**指定サーバー**(例: 1.1.1.1)だけを使うモードもあります。
- **Windows 設定を変更しない** — ネットワークアダプターの DNS 設定は変更しません。停止すれば痕跡は残らず、実行中に停電や強制終了があっても再起動すれば普段どおりに戻ります。
- **SNI 断片化** — HTTPS・HTTP 接続でサイト名が漏れないようにします。
- **例外リスト / 手動リスト** — 断片化しないサイト、または断片化するサイトだけを指定します。銀行・決済・ポータル・ゲームなど約 160 ドメインが既定の例外として組み込まれています。
- **ブラウザのみ** — 主要ブラウザの通信にだけ断片化を適用し、ゲームや業務ソフトには触れません。
- **偽装パケット** — 断片化だけでは足りない回線向けの追加の回避方式です。
- **切れない接続** — 開始前に DNS サーバーへ届くかを確認し、実行中にサーバーが応答しなくてもインターネットは切れず、サーバーが戻れば自動的に再開します。スリープ復帰や Wi-Fi 切替後もインターネットが止まりません。
- **レポート** — DNS・SNI 別にドメインと適用結果(暗号化、断片化、平文、サーバー経由 …)を表で表示し、コピーできます。
- **混合プロキシ** — リストに書いたドメインだけ海外サーバー経由で接続します。DNS では回避できない 451 ブロックのサイトを開くときに使い、それ以外は普段どおり直接接続するため速度の損失はありません。
- **自動起動・トレイ** — Windows 起動時に自動実行、閉じるときに通知領域へ最小化。
- **8 言語** — 韓国語・英語・日本語・中国語・ロシア語・イタリア語・フランス語・スペイン語。Windows の表示言語に従います。

## ダウンロード / インストール

| 種類 | リンク |
|---|---|
| インストーラー | [ダウンロード](https://down.kilho.net/secretdns?lang=ja) |
| ポータブル (ZIP) | [ダウンロード](https://down.kilho.net/secretdns?lang=ja&nosetup) |

インストーラー版はインストール完了後すぐに起動し、**Windows 起動時の自動実行**も一緒に登録されます。ポータブル版は ZIP を展開して `SecretDNS.exe` を実行するだけです。どちらも**管理者権限**を要求します。

2 つの違い: **混合プロキシ**機能はインストーラー版にのみ含まれます(ポータブル版では項目がロックされています)。

## 使い方

### 基本の流れ

1. SecretDNS を起動します。管理者権限の確認画面が出たら **[はい]** を押します。
2. ホーム画面の **[実行]** を押します。ボタンがしばらく**ネットワーク確認中**になった後**実行中**に変わり、通知領域(トレイ)のアイコンもオンの表示に変わります。
3. 普段どおりブラウザを使います。ほかにすることはありません。
4. オフにするには **[実行中]** ボタンをもう一度押すか、トレイアイコンを 1 回クリックします。ウィンドウを閉じるとプログラムは終了します(**終了時にトレイに最小化**をオンにしておくとトレイに入ります)。

何をオン/オフにするかは**設定**タブで決めます。実行中は設定がロックされるので、変更するには先に停止してください(混合プロキシのリストは例外 — 実行中でもすぐ反映されます)。

### 画面構成

上部に**ホーム・設定・レポート・寄付**タブがあり、右側のロゴを押すとホームページが開きます。

**設定**

| 項目 | 内容 |
|---|---|
| **DNS設定** — 無効 / 指定サーバー / DNS over HTTPS | DNS クエリの処理方法。**[…]** を押すとサーバーリストを編集する **DNSサーバー設定** ウィンドウが開きます |
| **SNI設定** — 無効 / 断片化 / 断片化ⓜ | 断片化をすべてのサイトに適用(例外リストを除く)するか、手動リストのサイトにだけ適用するか |
| **例外** / **手動** リスト | SNI 設定に応じて切り替わるドメインリスト。1 行に 1 つ書きます |
| **起動時にトレイに最小化** | ログオン時にトレイで自動開始(Windows 起動時の自動実行)。前回実行中だった場合は保護も自動でオンにします |
| **終了時にトレイに最小化** | ウィンドウを閉じても終了せずトレイに入ります |
| **混合プロキシを有効化** + **[…]** | リストのドメインだけ海外サーバー経由。**[…]** でリスト編集(インストーラー版、DNS 設定がオンである必要あり) |
| **レポートを有効化** | レポートタブに記録を残します |
| **ブラウザのみ** | ブラウザの通信にだけ断片化・偽装パケットを適用 |
| **偽装パケットを有効化** | 断片化だけでは足りない回線向けの追加の回避方式 |

**レポート** — **種別**(DNS・SNI・VPN)・**ドメイン**・**適用**の 3 列です。同じ行は 1 回だけ残り、適用欄が空の行(何も適用せず送出したもの)は薄く表示されます。**[コピー]**・**[コピー(全体)]** はタブ区切りテキストでコピーし、**[クリア]** はリストを消去します。

**トレイアイコン** — 1 回クリックで実行/停止が切り替わります。右クリックメニューには **SecretDNS**(ウィンドウ表示)・**実行**・**停止**・**Kilho.net**・**終了**があります。アイコンにマウスを載せるとバージョンと現在の状態が表示されます。

### こんなときは

**特定のサイトが警告ページに飛ばされる、または接続が切れる**
既定の設定(DNS **DNS over HTTPS** または**指定サーバー** + SNI **断片化**)で **[実行]** を押すだけでほとんど解決します。レポートを有効にしてそのサイトを開くと、`SNI` 行に**断片化**、`DNS` 行に **DNS over HTTPS** と付いていれば正常です。それでもだめなら下の**偽装パケットを有効化**を試してください。

**SecretDNS をオンにした後、あるサイトが開かなくなった(銀行・決済・ゲームのログインなど)**
そのサイトの通信が断片化に弱い場合です。設定の**例外**リストにドメインを 1 行追加して、もう一度実行してください。
- `.example.com` のように**ドットで始めて**書くと `example.com` とすべてのサブドメイン(`www.example.com`、`m.example.com`)に一致します。
- アドレスバーの URL をそのまま貼り付けても構いません — `https://`、`www.`、後ろのパスは自動的に取り除かれます。
- リストから別の場所をクリックすると保存され、**次回の実行から**適用されます。
- 銀行・カード・決済・ポータル・ショッピング・ゲーム・政府(`.go.kr`)・学校(`.ac.kr`)など約 160 ドメインはすでに例外として組み込まれているので、書く必要はありません。リストを空にしても組み込みの例外はそのまま働きます。

**いくつかのサイトだけ断片化して、ほかには触れたくない**
SNI 設定を**断片化ⓜ**に変えると、下のリストが**手動**リストになります。ここに書いたドメインだけを断片化し、それ以外はすべてそのまま送ります。問題のサイトが 1〜2 個だけなら、こちらの方が安全です。書き方のルールは例外リストと同じです。

**DNS だけ暗号化して断片化は使わない**
DNS 設定を **DNS over HTTPS**、SNI 設定を**無効**にします。逆に DNS はそのままで断片化だけ使うなら DNS を**無効**にしてください。両方無効だと「実行する機能がありません。」と表示されます。

**「暗号化(DoH) DNS サーバーに接続できません。」と表示される**
会社や学校の回線には、インターネットは使えるのに一部の DoH サーバーには接続できないところがあります。方法は 2 つです。
- DNS 設定の **[…]** を押し、**DNS over HTTPS** リストで別のサーバー(例: 既定リストの **cloudflare-dns.com**)にチェックを入れます。
- または DNS 設定を**指定サーバー**に変えます。暗号化はされませんが、Windows 設定を変えずに別の DNS サーバーを使う効果はそのままです。

**別の DNS サーバーを使いたい**
DNS 設定の横の **[…]** を押すと **DNSサーバー設定** ウィンドウが開きます。左が**指定サーバー**(IP アドレス)、右が **DNS over HTTPS** のリストです。
- **[追加]** で**名前・アドレス1・アドレス2**(予備、空でも可)を入力します。指定サーバーは `8.8.8.8` のような IPv4 アドレス、DoH は `https://1.1.1.1/dns-query` や `https://dns.google/dns-query` のようなアドレスです。
- 項目の前の**チェック**を入れたサーバーだけを使います。複数にチェックを入れると、1 つが応答しないとき別のサーバーへ自動的に切り替わります。1 つもチェックしていない場合は Cloudflare を使います。
- 既定リストには Cloudflare(チェック済み)と Google(チェックなし)が入っています。
- 変更したリストは**次回の実行から**適用されます。

**ゲームや業務ソフトに影響を与えたくない**
**ブラウザのみ**をオンにしてください。断片化と偽装パケットを Chrome・Edge・Firefox・Whale などの主要ブラウザの通信にだけ適用し、ほかのプログラムには触れません。DNS の暗号化はプログラムに関係なく適用され続けます。

**断片化してもまだブロックされる回線**
**偽装パケットを有効化**を試してください。断片化だけでは通り抜けられない検閲装置向けの追加の回避方式で、実際の接続には影響しません。

**ブロックされたサイトだけ海外サーバー経由で接続したい(混合プロキシ)**
インターネットの自由が保障されていない国では、サイト側がその国からの接続自体を拒否し、ブラウザに **451 エラー**(Unavailable For Legal Reasons)が表示されることがあります。自由なインターネットのある国ではほとんど見られないケースです。この場合は DNS の暗号化や断片化では開けません — 接続元の国を見てブロックしているためです。混合プロキシはそうしたサイトだけを他国のサーバー経由で接続させます。
インストーラー版で DNS 設定をオンにした状態で**混合プロキシを有効化**にチェックを入れ、横の **[…]** でリストウィンドウを開きます。1 行に 1 ドメイン(`.example.com`、例外リストと同じルール)を書いて **[OK]** を押します。
- リストのドメインだけ海外サーバーを経由し、それ以外は普段どおり直接接続します。VPN の全体接続と違い、ほかのサイトの速度はそのままです。
- サーバーは実行のたびに自動で割り当てられ、応答がなければ次のサーバーへ自動的に切り替わります。
- リストは**実行中でも**すぐ反映されます。
- レポートには種別 **VPN**、適用**サーバー経由**として残ります。
- 混合プロキシは DNS 設定がオンである必要があり、DNS を無効にすると一緒にオフになります。

**コンピューターを起動するたびに自動でオンにしておきたい**
**起動時にトレイに最小化**にチェックを入れます(インストーラー版はインストール時にすでに登録されています)。ログオンするとウィンドウなしでトレイから開始し、**前回実行中の状態にしていた場合**は保護も自動でオンにします。自分で停止して終了した場合は、次回起動時はオンにせず待機します。
起動直後に Wi-Fi の接続が遅れても、ネットワークがつながれば自動で開始します。

**ウィンドウを閉じてもオンのままにしておきたい**
**終了時にトレイに最小化**をオンにすると、閉じる(×)を押しても終了せずトレイに入ります。完全に終了するにはトレイアイコンを右クリック → **終了**(確認画面が出ます)。終了すると保護も一緒にオフになります。

**今どのサイトがどう処理されているか見たい**
**レポートを有効化**をオンにして**レポート**タブを開きます。実行中にアクセスしたドメインが 1 行ずつたまります。

| 種別 | 適用 | 意味 |
|---|---|---|
| DNS | **DNS over HTTPS** | 暗号化サーバーで問い合わせました |
| DNS | **平文** | 指定サーバーで問い合わせました(暗号化なし) |
| SNI | **断片化** | サイト名を断片化して送りました |
| SNI | (空欄) | 例外リストのサイトなのでそのまま送りました |
| VPN | **サーバー経由** | 混合プロキシで海外サーバーを経由しました |

**[コピー(全体)]** でコピーしてメモ帳や Excel に貼り付けると列がそのまま分かれます。

**DNS サーバーが応答しなくてもインターネットが切れない理由**
SecretDNS は実行前に DNS サーバーへ届くかを先に確認し、実行中にサーバーがしばらく応答しなくてもインターネットが止まらないよう自動で処理します。サーバーが戻れば自動的に元の動作に戻り、その間の状態はトレイアイコンとレポートで確認できます。スリープから復帰したり Wi-Fi を切り替えたりしても、改めて実行し直す必要はありません。

**ドライバーの案内画面が表示される**
まれに、コンピューターを再起動してから実行するよう求める案内が出ることがあります。案内どおり再起動すれば大丈夫です。

**実行中にコンピューターが落ちた**
心配ありません。SecretDNS は Windows のネットワーク設定を変えず実行中だけ動作するので、停電や強制終了の後にもう一度起動すればインターネットは普段どおりです。自動起動をオンにしていれば、ログオン後に自動で再開します。

## 設定

**設定**タブで変更し、変更するとすぐ保存されます。ほとんどは**次回の実行から**適用され、アップデートしても設定は維持されます。

| 項目 | 既定値(インストーラー版) | 既定値(ポータブル版) |
|---|---|---|
| SNI設定 | 断片化 | 断片化 |
| DNS サーバーリスト | 指定サーバー: Cloudflare ✓, Google / DoH: Cloudflare ✓, cloudflare-dns.com ✓, Google | 同じ |
| 起動時にトレイに最小化 | オン | オフ |
| 終了時にトレイに最小化 | オン | オフ |
| 混合プロキシを有効化 | オフ | 使用不可 |
| レポートを有効化 | オフ | オフ |
| ブラウザのみ | オフ | オフ |
| 偽装パケットを有効化 | オフ | オフ |

表示言語は Windows の表示言語に従います(韓国語・英語・日本語・中国語・ロシア語・イタリア語・フランス語・スペイン語、それ以外は英語)。

## 動作環境

- Windows 10 または Windows 11(32 ビット・64 ビットの両方)
- **管理者権限** — 起動のたびに権限の確認画面が表示されます。
- 追加のランタイムは不要です。
- インターネット接続 — DNS サーバーの確認と新バージョンの案内に使います。

## アップデート

SecretDNS は自動的にはアップデート**しません**。起動時に新バージョンの有無を確認して案内画面を表示し、**[はい]** を押すとダウンロードページが開いてプログラムが終了します。新バージョンは内部検証の後に手動で配布され、[SecretDNS ページ](https://v2.kilho.net/secretdns)で告知されます。[アップデートポリシー](https://en.kilho.net/archives/notice/2940)をご覧ください。

**バージョン履歴**

| バージョン | 日付 | 内容 |
|---|---|---|
| 4.0.5 | 2026-09-15 | 混合プロキシのサーバー自動切替で接続の安定性向上、レポートで VPN 利用履歴を確認しやすく |
| 4.0.4 | 2026-09-14 | アップデート・自動起動の安定化、混合プロキシの刷新(ドメインリスト編集画面、設定の保存)、好きな DNS サーバーを追加・選択できる設定画面、一部通信事業者での暗号化 DNS 接続の改善(ホスト名アドレス対応、Cloudflare 接続先の追加)、接続失敗時に別サーバーを案内、レポート文言の整理 |
| 4.0.2 | 2026-09-04 | 一部環境でインターネットが断続的に止まる問題を解決、不安定な DNS サーバーの自動切替、一時的な DNS エラーでもサイトが開き続けるよう改善、ネットワークが安定したら暗号化接続を自動復旧 |
| 4.0.1 | 2026-08-25 | 起動直後にインターネットにつながらない問題を修正、スリープ・VPN・Wi-Fi 切替後の接続問題を改善、インターネット障害時の自動復旧、接続待機・復旧状態の表示 |

## ライセンス

SecretDNS は**フリーウェア**です。会社、自宅、官公庁、学校など、どこでも制限なく無料で使え、自由に再配布できます。

## リンク

- Web サイト: <https://v2.kilho.net/secretdns>
- フォーラム: <https://groups.google.com/g/kilhonet>
- X (Twitter): <https://www.twitter.com/kilhonet>

© KILHO.NET
